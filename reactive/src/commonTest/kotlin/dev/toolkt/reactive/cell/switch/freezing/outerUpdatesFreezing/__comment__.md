# The outer cell updates freezing

The outer cell updates freezing and the updated inner cell is frozen / freezing (*).

The result cell should update.

The result cell should freeze.

(*) The case when the outer cell updates freezing and the updated inner cell is warm is handled from the perspective of the
outer cell update.
